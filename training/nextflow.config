docker.enabled = true
docker.runOptions = '-u $(id -u):$(id -g)'

params {
    max_cpus   = 2
    max_memory = '6.5GB'
    max_time   = '2.h'
    igenomes_base = '/workspace/gitpod/training/data/refs/'

}
